# 일기 Mock 데이터 생성 스크립트

페이지네이션 테스트를 위한 일기 mock 데이터를 생성하는 스크립트입니다.

## 📁 파일 설명

### 1. `generate-diary-mock-data.js` (권장)
다양한 기능을 제공하는 완전한 버전입니다.

### 2. `quick-generate-diaries.js`
빠르게 한 번에 실행할 수 있는 간소화 버전입니다.

## 🚀 사용 방법

### 방법 1: 전체 기능 버전 사용 (권장)

1. 브라우저에서 개발자 도구를 엽니다 (F12 또는 Cmd+Option+I)
2. Console 탭으로 이동합니다
3. `generate-diary-mock-data.js` 파일의 전체 내용을 복사합니다
4. 콘솔에 붙여넣고 Enter를 누릅니다
5. 아래 명령어를 실행합니다:

```javascript
// 120개의 일기 데이터 생성 및 저장
DiaryMockData.generate(120)

// 현재 저장된 데이터 확인
DiaryMockData.check()

// 저장된 데이터 삭제
DiaryMockData.clear()
```

### 방법 2: 빠른 실행 버전 사용

1. 브라우저에서 개발자 도구를 엽니다
2. Console 탭으로 이동합니다
3. `quick-generate-diaries.js` 파일의 전체 내용을 복사합니다
4. 콘솔에 붙여넣고 Enter를 누릅니다
5. 자동으로 120개의 데이터가 생성됩니다

## 📊 생성되는 데이터 구조

```typescript
interface DiaryItem {
  id: number;           // 고유 ID (1부터 시작)
  title: string;        // 일기 제목
  content: string;      // 일기 내용
  emotion: EmotionType; // 감정 타입
  createdAt: string;    // 생성 날짜 (ISO 8601)
}

type EmotionType = 'Happy' | 'Sad' | 'Angry' | 'Surprise' | 'Etc';
```

## 🎯 주요 특징

- ✅ 120개의 다양한 일기 데이터 생성
- ✅ 5가지 감정 타입 골고루 분포
- ✅ 최근 6개월 내 랜덤 날짜 생성
- ✅ 최신순으로 자동 정렬
- ✅ 로컬 스토리지에 자동 저장
- ✅ 페이지네이션 테스트에 최적화

## 📝 사용 예시

### 데이터 생성 및 확인
```javascript
// 1. 스크립트 로드 후
DiaryMockData.generate(120)
// ✅ 120개의 일기 mock 데이터가 생성되어 로컬 스토리지에 저장되었습니다!
// 📊 데이터 통계:
// ┌─────────┬────────┐
// │  (index) │ Values │
// ├─────────┼────────┤
// │  Happy   │   24   │
// │   Sad    │   23   │
// │  Angry   │   25   │
// │ Surprise │   24   │
// │   Etc    │   24   │
// └─────────┴────────┘

// 2. 데이터 확인
DiaryMockData.check()
// 📦 현재 로컬 스토리지에 120개의 일기가 저장되어 있습니다.

// 3. 원하는 개수만큼 생성
DiaryMockData.generate(200) // 200개 생성

// 4. 데이터 삭제
DiaryMockData.clear()
// 🗑️ 로컬 스토리지의 일기 데이터가 삭제되었습니다.
```

## 🧪 페이지네이션 테스트

생성된 데이터로 다음을 테스트할 수 있습니다:

- ✅ 페이지 이동 기능
- ✅ 한 페이지당 표시되는 아이템 수
- ✅ 전체 페이지 수 계산
- ✅ 첫 페이지/마지막 페이지 이동
- ✅ 이전/다음 페이지 버튼
- ✅ 감정별 필터링 후 페이지네이션
- ✅ 검색 후 페이지네이션

## 💡 팁

### 다른 개수로 생성하기
```javascript
DiaryMockData.generate(50)   // 50개 생성
DiaryMockData.generate(150)  // 150개 생성
DiaryMockData.generate(300)  // 300개 생성
```

### 데이터만 생성하고 저장하지 않기
```javascript
const diaries = DiaryMockData.generateOnly(120);
console.log(diaries); // 생성된 데이터 확인
```

### 특정 감정만 있는지 확인
```javascript
const stored = JSON.parse(localStorage.getItem('diaries'));
const happyDiaries = stored.filter(d => d.emotion === 'Happy');
console.log(`행복한 일기: ${happyDiaries.length}개`);
```

## ⚠️ 주의사항

- 스크립트 실행 시 기존 로컬 스토리지의 'diaries' 데이터가 **완전히 덮어씌워집니다**
- 중요한 데이터가 있다면 먼저 백업하세요
- 페이지를 새로고침하면 변경사항이 반영됩니다

## 🔄 데이터 백업 및 복구

### 백업
```javascript
const backup = localStorage.getItem('diaries');
console.log('백업 완료:', backup);
// 이 값을 복사해서 저장하세요
```

### 복구
```javascript
const backupData = '...'; // 백업한 데이터 붙여넣기
localStorage.setItem('diaries', backupData);
console.log('복구 완료!');
```

## 📞 문제 해결

### 데이터가 보이지 않을 때
1. 페이지를 새로고침합니다
2. 콘솔에서 `DiaryMockData.check()`로 데이터 확인
3. 브라우저 로컬 스토리지 확인 (개발자 도구 > Application > Local Storage)

### 오류 발생 시
1. 콘솔의 오류 메시지 확인
2. 스크립트를 다시 붙여넣고 실행
3. 브라우저 캐시 삭제 후 재시도

## 📚 관련 파일

- `/src/components/diaries/index.tsx` - 일기 목록 컴포넌트
- `/src/components/diaries/hooks/index.filter.hook.ts` - 필터 로직
- `/src/commons/constants/enum.ts` - 감정 타입 정의

---

**Created for vibe-coding project**

